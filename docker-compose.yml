services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: intentloop
      POSTGRES_PASSWORD: intentloop
      POSTGRES_DB: intentloop
    ports:
      - "5432:5432"
    volumes:
      - intentloop_pg:/var/lib/postgresql/data
volumes:
  intentloop_pg:
